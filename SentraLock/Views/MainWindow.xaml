<Window x:Class="SentraLock.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:fa="clr-namespace:FontAwesome.Sharp;assembly=FontAwesome.Sharp"
        xmlns:local="clr-namespace:SentraLock"
		xmlns:viewModel="clr-namespace:SentraLock.ViewModels"
		xmlns:views="clr-namespace:SentraLock.Views"
        mc:Ignorable="d"
        Title="SentraLock" Height="200" Width="450" ResizeMode="NoResize" WindowStartupLocation="CenterScreen" WindowStyle="None">
	<Window.DataContext>
		<viewModel:MainViewModel/>
	</Window.DataContext>
	<Window.Resources>
		<DataTemplate DataType="{x:Type viewModel:MenuViewModel}">
			<views:MenuView/>
		</DataTemplate>
		<DataTemplate DataType="{x:Type viewModel:EncryptViewModel}">
			<views:EncryptView/>
		</DataTemplate>
		<DataTemplate DataType="{x:Type viewModel:DecryptViewModel}">
			<views:DecryptView/>
		</DataTemplate>
	</Window.Resources>
	<Border>
		<Border BorderThickness="1">
			<Border.BorderBrush>
				<LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
					<GradientStop Color="{StaticResource secondaryColor}" Offset="0"/>
					<GradientStop Color="{StaticResource supportColor}" Offset="1"/>
				</LinearGradientBrush>
			</Border.BorderBrush>
			<Grid>
				<Grid.RowDefinitions>
					<RowDefinition Height="25"/>
					<RowDefinition Height="*"/>
				</Grid.RowDefinitions>
				<Grid Grid.Row="0">
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="*"/>
						<ColumnDefinition Width="*"/>
					</Grid.ColumnDefinitions>
					<StackPanel Grid.Column="0" Orientation="Horizontal">
						<fa:IconImage Icon="Lock" Foreground="{StaticResource logoColorBrush}" MaxHeight="30" VerticalAlignment="Center" Margin="5,5,0,5"/>
						<TextBlock Text="SentraLock" FontFamily="{StaticResource Poppins-Black}" FontWeight="Black" Foreground="{StaticResource secondarySolidBrush}" MaxHeight="30" VerticalAlignment="Center" Margin="5,5,0,5"/>
					</StackPanel>
					<StackPanel Grid.Column="1" Orientation="Horizontal" FlowDirection="RightToLeft">
						<Button x:Name="closeButton" Content="X" FontFamily="{StaticResource Poppins-Black}" FontWeight="Black" Width="25" Style="{StaticResource buttonClose}" Cursor="Hand" Command="{Binding CloseApplicationCommand}"/>
					</StackPanel>
				</Grid>
				<Grid Grid.Row="1">
					<ContentControl Content="{Binding CurrentChildView}"/>
				</Grid>
			</Grid>
		</Border>
	</Border>
	<Window.Background>
		<LinearGradientBrush StartPoint="0,1" EndPoint="1,0">
			<GradientStop Color="{StaticResource supportColor}" Offset="0"/>
			<GradientStop Color="{StaticResource secondaryColor}" Offset="1"/>
		</LinearGradientBrush>
	</Window.Background>
</Window>
